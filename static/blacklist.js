// JavaScript functions for blacklist actions
// Note: studentId is passed as a string from the HTML onclick attribute
function viewDetails(studentId) {
    alert('Viewing details for student ID: ' + studentId + ' (Feature coming soon!)');
}

function markResolved(studentId) {
    alert('Marking student ID: ' + studentId + ' as resolved (Feature coming soon!)');
}

function exportBlacklist() {
    alert('Export feature coming soon! (e.g., download CSV)');
}

function notifyParents() {
    alert('Notification feature coming soon! (e.g., send emails)');
}